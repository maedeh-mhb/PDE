import React from 'react';
import search from '../../../assets/Icons/search.svg';

const SearchBox = () => {
    return (
            <>
                 <img src={search} />
            </>
    );
};

export default SearchBox;